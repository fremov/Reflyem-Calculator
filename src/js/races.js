const raceArgo = document.getElementById('raceArgo');
const raceOrc = document.getElementById('raceOrc');
const raceNord = document.getElementById('raceNord');
const raceRedgarc = document.getElementById('raceRedgarc');
const raceDanmer = document.getElementById('raceDanmer');
const raceImperec = document.getElementById('raceImperec');
const raceBosmer = document.getElementById('raceBosmer');
const raceCatjit = document.getElementById('raceCatjit');
const raceAltmer = document.getElementById('raceAltmer');
const raceBreton = document.getElementById('raceBreton');

export default {
    raceArgo,
    raceOrc,
    raceNord,
    raceRedgarc,
    raceDanmer,
    raceImperec,
    raceBosmer,
    raceCatjit,
    raceAltmer,
    raceBreton,
}

